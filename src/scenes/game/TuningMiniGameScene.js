import Scene from '../scene'

export default class TuningMiniGameScene extends Scene {
  constructor () {
    super({key: 'TuningMiniGameScene'})
  }

  create (params) {
    super.create(params)

    // TODO: create the world for the scene and attach key listeners
  }
}